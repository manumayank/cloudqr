import { Module } from '@nestjs/common';

@Module({
  // TODO: Implement campaigns module
  // - POST /campaigns - Create campaign with QR code
  // - GET /campaigns - List user's campaigns
  // - GET /campaigns/:id - Get campaign details
  // - PUT /campaigns/:id - Update campaign (updates redirect target)
  // - DELETE /campaigns/:id - Soft delete campaign
  // - POST /campaigns/:id/qr-codes - Generate additional QR codes
})
export class CampaignsModule {}
